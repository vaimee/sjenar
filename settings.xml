<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0">
  
  <!-- Repositories di Maven -->
  <mirrors>
    <mirror>
      <id>central</id>
      <mirrorOf>central</mirrorOf>
      <url>https://repo.maven.apache.org/maven2</url>
      <blocked>false</blocked>
    </mirror>
  </mirrors>

  <!-- Repositori per pacchetti privati, se necessario -->
  <servers>
    <server>
      <id>github-packages</id>
      <username>${env.GITHUB_ACTOR}</username>
      <password>${env.GITHUB_TOKEN}</password>
    </server>
  </servers>

  <!-- Configurazioni per i plugin di Maven -->
  <profiles>
    <profile>
      <id>github-actions</id>
      <repositories>
        <repository>
          <id>github-releases</id>
          <url>https://maven.pkg.github.com/vaimee/sjenar</url>
          <releases>
            <enabled>true</enabled>
          </releases>
        </repository>
      </repositories>
    </profile>
  </profiles>

  <!-- Attivare i profili specifici -->
  <activeProfiles>
    <activeProfile>github-actions</activeProfile>
  </activeProfiles>

</settings>
